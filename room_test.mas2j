MAS room_experiment {

	infrastructure: Centralised

	environment: RoomEnvironmentLocal_Inst
	//environment: RoomEnvironmentTimeStep

	agents:
		coordinator[mindinspector="gui(cycle,html,history)"];
		//coordinator agentArchClass  SomewhatTransparentAgents.JASON.STAgArch;
		synthesizer [mindinspector="gui(cycle,html,history)"] #2;
		//synthesizer agentArchClass  SomewhatTransparentAgents.JASON.STAgArch #2;
		//agentArchClass room_experiment.STAgArch;
// 	    beliefBaseClass SomewhatTransparentAgents.JASON.STBelBase;
		//sample_agent #2; // [mindinspector="gui(cycle,html,history)"] #2;
		agent [mindinspector="gui(cycle,html,history)"]#6; //agentArchClass jason_logger.LoggerAgArch;
//		agentArchClass  SomewhatTransparentAgents.JASON.STAgArch
//  	    beliefBaseClass SomewhatTransparentAgents.JASON.STBelBase #8;
		sample sample_agent;
		synthesizer_test syn_test[mindinspector="gui(cycle,html,history)"];
		bAgent[mindinspector="gui(cycle,html,history)"]#6;
		pAgent[mindinspector="gui(cycle,html,history)"] #2;
		
		
	aslSourcePath:
		"src/asl";
}