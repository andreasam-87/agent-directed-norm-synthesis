{
"R1" : "occurred(deniedExit(P,L),rooms,I) :- occurred(leave(P,L),rooms,I),not occurred(viol(leave(P,L)),rooms,I),

   not
   holdsat(permExit(P,L),rooms,_,I),
   person(P),
   location(L),
   inst(rooms), instant(I).",
   
"R2": "occurred(exit(P,L),rooms,I) :- occurred(leave(P,L),rooms,I),not occurred(viol(leave(P,L)),rooms,I),

   holdsat(permExit(P,L),rooms,_,I),
   person(P),
   location(L),
   inst(rooms), instant(I).",
   
"R3": "occurred(capacityExceeded(L),rooms,I) :- occurred(arrive(P,L),rooms,I),not occurred(viol(arrive(P,L)),rooms,I),

   holdsat(occupancy(L,X),rooms,_,I),
   holdsat(max(L,Y),rooms,_,I),
   holdsat(equal(X,Y),rooms,_,I),
   person(P),
   number(Y),
   location(L),
   number(X),
   inst(rooms), instant(I).",
   
"R4": "occurred(capacityExceeded(L),rooms,I) :- occurred(arrive(P,L),rooms,I),not occurred(viol(arrive(P,L)),rooms,I),

   holdsat(occupancy(L,X),rooms,_,I),
   holdsat(max(L,Y),rooms,_,I),
   holdsat(bigger(X,Y),rooms,_,I),
   person(P),
   number(Y),
   location(L),
   number(X),
   inst(rooms), instant(I).",
   
"R5": "occurred(deniedEntry(P,L),rooms,I) :- occurred(enter(P,L),rooms,I),not occurred(viol(enter(P,L)),rooms,I),

   not
   holdsat(permEntry(P,L),rooms,_,I),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R6": "occurred(arrive(P,L),rooms,I) :- occurred(enter(P,L),rooms,I),not occurred(viol(enter(P,L)),rooms,I),

   holdsat(permEntry(P,L),rooms,_,I),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R7": "initiated(capacityExceededViol(L),rooms,capacityExceeded(L),I) :-
   occurred(capacityExceeded(L),rooms,I),
   not occurred(viol(capacityExceeded(L)),rooms,I),
   not holdsat(meeting,rooms,_,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   location(L),
   inst(rooms), instant(I).",


"R8": "initiated(perm(enter(P,L)),rooms,exit(P,L),I) :-
   occurred(exit(P,L),rooms,I),
   not occurred(viol(exit(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R9": "initiated(pow(enter(P,L)),rooms,exit(P,L),I) :-
   occurred(exit(P,L),rooms,I),
   not occurred(viol(exit(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R10": "initiated(perm(arrive(P,L)),rooms,exit(P,L),I) :-
   occurred(exit(P,L),rooms,I),
   not occurred(viol(exit(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R11": "initiated(perm(leave(P,L)),rooms,arrive(P,L),I) :-
   occurred(arrive(P,L),rooms,I),
   not occurred(viol(arrive(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R12": "initiated(pow(leave(P,L)),rooms,arrive(P,L),I) :-
   occurred(arrive(P,L),rooms,I),
   not occurred(viol(arrive(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R13": "initiated(perm(exit(P,L)),rooms,arrive(P,L),I) :-
   occurred(arrive(P,L),rooms,I),
   not occurred(viol(arrive(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R14": "initiated(in_room(P,L),rooms,arrive(P,L),I) :-
   occurred(arrive(P,L),rooms,I),
   not occurred(viol(arrive(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",


"R15": "terminated(in_room(P,L),rooms,exit(P,L),I) :-
   occurred(exit(P,L),rooms,I),
   not occurred(viol(exit(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R16": "terminated(perm(leave(P,L)),rooms,exit(P,L),I) :-
   occurred(exit(P,L),rooms,I),
   not occurred(viol(exit(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R17": "terminated(pow(leave(P,L)),rooms,exit(P,L),I) :-
   occurred(exit(P,L),rooms,I),
   not occurred(viol(exit(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R18": "terminated(perm(exit(P,L)),rooms,exit(P,L),I) :-
   occurred(exit(P,L),rooms,I),
   not occurred(viol(exit(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R19": "terminated(perm(enter(P,L)),rooms,arrive(P,L),I) :-
   occurred(arrive(P,L),rooms,I),
   not occurred(viol(arrive(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R20": "terminated(pow(enter(P,L)),rooms,arrive(P,L),I) :-
   occurred(arrive(P,L),rooms,I),
   not occurred(viol(arrive(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R21": "terminated(perm(arrive(P,L)),rooms,arrive(P,L),I) :-
   occurred(arrive(P,L),rooms,I),
   not occurred(viol(arrive(P,L)),rooms,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   person(P),
   location(L),
   inst(rooms), instant(I).",
   
"R22": "holdsat(typeCount(L,R,N),rooms,nif,I) :- N = #count { P: 
 holdsat(in_room(P,L),rooms,_,I),
 holdsat(role(P,R),rooms,_,I) },
   role(R),
   location(L),
   number(N),
   inst(rooms), instant(I).",
 
"R23": "holdsat(occupancy(L,N),rooms,nif,I) :- N = #count { P: 
 holdsat(in_room(P,L),rooms,_,I) },
   location(L),
   number(N),
   inst(rooms), instant(I).",

"R24": "holdsat(potential_role_conflict(L),rooms,nif,I) :-
   holdsat(potential_occupant_conflict(L),rooms,_,I),
   holdsat(typeCount(L,R,2),rooms,_,I),
   location(L),
   inst(rooms), instant(I).",

"R25": "holdsat(potential_occupant_conflict(L),rooms,nif,I) :-
   holdsat(occupancy(L,3),rooms,_,I),
   location(L),
   inst(rooms), instant(I).",

"R26": "holdsat(permExit(P,L),rooms,nif,I) :-
   holdsat(in_room(P,L),rooms,_,I),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R27": "holdsat(permEntry(P,L),rooms,nif,I) :-
   not
   holdsat(in_some_room(P),rooms,_,I),
   person(P),
   location(L),
   inst(rooms), instant(I).",

"R28": "holdsat(in_some_room(P),rooms,nif,I) :-
   holdsat(in_room(P,L),rooms,_,I),
   person(P),
   inst(rooms), instant(I).",
   
 "M1R7": "initiated(capacityExceededViol(L),rooms,capacityExceeded(L),I) :-
   occurred(capacityExceeded(L),rooms,I),
   not occurred(viol(capacityExceeded(L)),rooms,I),
   not holdsat(meeting,rooms,_,I),
   holdsat(live(rooms),rooms,_create_,I),inst(rooms),
   location(L),
   inst(rooms), instant(I)."

}