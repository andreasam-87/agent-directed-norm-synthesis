MAS room_experiment {

	infrastructure: Centralised

	environment: RoomEnvironmentLocal_Inst
	//environment: RoomEnvironmentTimeStep

	agents:
		coordinator[mindinspector="gui(cycle,html,history)"];
		//coordinator agentArchClass  SomewhatTransparentAgents.JASON.STAgArch;
		synthesizer [mindinspector="gui(cycle,html,history)"]#2;
		
		//agent [mindinspector="gui(cycle,html,history)"]#8; 

		//base_agent[mindinspector="gui(cycle,html,history)"]#2;
		//baseAgent[mindinspector="gui(cycle,html,history)"]#4;
	    //baseAgent[mindinspector="gui(cycle,html,history)"]#8;
	    
	    
		sample sample_agent;
		
		//Setup for the final set of experiments
		basePlanner[mindinspector="gui(cycle,html,history)"]#3;
		basePlannerVIP[mindinspector="gui(cycle,html,history)"];
		vip[mindinspector="gui(cycle,html,history)"]#2;
		planner[mindinspector="gui(cycle,html,history)"] #2;
		
		//bAgent[mindinspector="gui(cycle,html,history)"]#2;
		
		//boldAgent[mindinspector="gui(cycle,html,history)"]#2;
		//cBoldAgent[mindinspector="gui(cycle,html,history)"]#2;
		//pAgent[mindinspector="gui(cycle,html,history)"] #2;
		//planAgent[mindinspector="gui(cycle,html,history)"] #2;
		
		oracle;
		//vipAgent[mindinspector="gui(cycle,html,history)"]#8;
		
	aslSourcePath:
		"src/asl";
}